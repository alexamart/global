# Global Admin UI

This is the React/Vite admin app for the Global backend.

## Setup

1. Run `npm install`
2. Run `npm run dev`
3. Open `http://localhost:5173`

The app proxies `/api` requests to `http://localhost:4000`.
